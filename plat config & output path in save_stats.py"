[1mdiff --git a/save_stats.py b/save_stats.py[m
[1mindex de38659..692f1b0 100644[m
[1m--- a/save_stats.py[m
[1m+++ b/save_stats.py[m
[36m@@ -48,7 +48,7 @@[m [mfrom nerfview import CameraState, RenderTabState, apply_float_colormap[m
 [m
 @dataclass[m
 class Config:[m
[31m-    pt_path: str[m
[32m+[m[32m    pt_path: str = ""[m
     # Disable viewer[m
     disable_viewer: bool = False[m
     # Path to the .pt files. If provide, it will skip training and run evaluation only.[m
[36m@@ -1367,23 +1367,23 @@[m [mdef main(local_rank: int, world_rank, world_size: int, cfg: Config):[m
 [m
     if cfg.save_first_ckp:[m
         runner.save_ckpt()[m
[31m-        ckpts = [[m
[31m-            torch.load(file, map_location=runner.device, weights_only=True)[m
[31m-            for file in cfg.ckpt[m
[31m-        ][m
[31m-        for k in runner.splats.keys():[m
[31m-            runner.splats[k].data = torch.cat([ckpt["splats"][k] for ckpt in ckpts])[m
[31m-        step = ckpts[0]["step"][m
[31m-        runner.eval(step=step)[m
[31m-        runner.render_traj(step=step)[m
[31m-        if cfg.compression is not None:[m
[31m-            runner.run_compression(step=step)[m
[32m+[m[32m        # ckpts = [[m
[32m+[m[32m        #     torch.load(file, map_location=runner.device, weights_only=True)[m
[32m+[m[32m        #     for file in cfg.ckpt[m
[32m+[m[32m        # ][m
[32m+[m[32m        # for k in runner.splats.keys():[m
[32m+[m[32m        #     runner.splats[k].data = torch.cat([ckpt["splats"][k] for ckpt in ckpts])[m
[32m+[m[32m        # step = ckpts[0]["step"][m
[32m+[m[32m        # runner.eval(step=step)[m
[32m+[m[32m        # runner.render_traj(step=step)[m
[32m+[m[32m        # if cfg.compression is not None:[m
[32m+[m[32m        #     runner.run_compression(step=step)[m
 [m
     runner.train()[m
 [m
     Path(cfg.result_dir).mkdir(exist_ok=True)[m
[31m-    # TOCHANGE[m
[31m-    with open("gs_out/canny_500k.json", "w", encoding="utf-8") as f:[m
[32m+[m[32m    out_path = os.path.join(cfg.result_dir, "gsplat_stats.json")[m
[32m+[m[32m    with open(out_path, "w", encoding="utf-8") as f:[m
         json.dump(runner.stats_arr, f, indent=2, ensure_ascii=False)[m
 [m
     if not cfg.disable_viewer:[m
